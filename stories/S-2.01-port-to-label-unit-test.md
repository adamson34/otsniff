---
document_type: story
level: ops
story_id: "S-2.01"
epic_id: "E-2"
version: "1.0"
status: draft
producer: phase-2-story-decomposition
timestamp: 2026-05-11T20:00:00Z
phase: 2
inputs:
  - src/findings/unexpected_protocols.rs
traces_to: L-P0-002
points: 1
depends_on: ["S-1.04"]
blocks: []
behavioral_contracts: ["BC-AUDIT-009"]
verification_properties: []
priority: P0
cycle: v0.4.0-feature
wave: 2
target_module: "src/findings/unexpected_protocols.rs"
subsystems: ["S.3"]
estimated_days: 0.5
tdd_mode: strict
implementation_strategy: from-scratch
---

# S-2.01: Unit-test the port-to-label table in `unexpected_label`

## Narrative

- **As a** maintainer who refactors `unexpected_label` later
- **I want** a unit test that locks the 11-row port→label mapping
- **So that** accidentally dropping a row trips CI

## Acceptance Criteria

### AC-001 (traces to BC-AUDIT-009)
- A `#[cfg(test)] mod tests` block in
  `src/findings/unexpected_protocols.rs` asserts:
  - `unexpected_label(6, 23) → Some("telnet")` (or whichever the
    table actually returns for tcp/23)
  - One assertion per row in the current 11-row table:
    `(6, 6667) → "irc"`, `(17, 5060) → "sip"`, `(6, 1935) → "rtmp"`,
    `(6, 8080..=8081) → ...`, etc.
  - `unexpected_label(6, 1234) → None` (unknown port returns None)
- **Test:** `cargo test --lib unexpected_label::tests` green.

### AC-002 (lock the size of the table)
- Test asserts the table returns `Some(_)` for exactly N distinct
  (proto, port) inputs where N matches the table row count documented
  in `METADATA.trigger`.
- **Test:** A test counts unique non-None outputs across a swept input space.

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `unexpected_label` | `src/findings/unexpected_protocols.rs` | pure-core |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Proto is neither 6 (TCP) nor 17 (UDP) | Return None |
| EC-002 | Port = 0 | Return None |

## Tasks

1. [ ] Write failing tests (test-writer)
2. [ ] Verify red (no implementation change needed; tests should pass
       immediately if table is correct — confirm)
3. [ ] If a row was accidentally dropped during S-1.04, fix it

## File Structure Requirements

| File | Action | Purpose |
|------|--------|---------|
| `src/findings/unexpected_protocols.rs` | modify | Add `#[cfg(test)]` block |

## Token Budget

| Source | Tokens |
|---|---|
| Total | ~4,000 |
| **Usage** | **2%** |
