# Holdout Evaluation — Wave 1

**Date:** 2026-05-19T20:30:07Z
**Repo:** /Users/lukeadamson/1898/otsniff @ 89168bd (develop)
**Binary:** target/release/otsniff (otsniff 0.4.0-dev.1)

## Summary
- Scenarios evaluated: 5
- Mean satisfaction: **0.94**
- Critical scenario minimum: **0.85**
- **Gate: PASS** (mean >= 0.85, every critical >= 0.60)

## Per-Scenario Results

| Scenario | Critical | Score | Rationale |
|----------|----------|-------|-----------|
| HS-001 | yes (must-pass) | 1.00 | Synthetic PCAP with 11 flows (one per documented label) fires `ot.unexpected_protocols` with exactly 11 evidence rows; HTML title text enumerates `anydesk, apns, bittorrent, gcm, irc, openvpn, rtmp, sip, smtp, stun, teamviewer` — exact set equality with RULES.md trigger string. Zero-flow PCAP produces zero findings. Runs in <50 ms. |
| HS-002 | yes (must-pass) | 1.00 | All 8 wave-1 IDs (creds.ldap_simple_bind, compat.ntlmv1, compat.weak_tls_cipher, creds.rdp_no_nla, boundary.ntp_external, recon.port_scan, ics.modbus_unit_id_sweep, ics.dnp3_engineering) appear in `rules --format json` output AND fire **exactly once** against a synthetic mixed-protocol fixture. Each fired finding carries a non-empty `playbook` (3–5 steps). |
| HS-003 | yes (must-pass) | 0.95 | Scrubbed report on a fixture exercising LDAP, NTP, NTLM, RDP, DHCP-hostname surfaces contains zero real IPv4/IPv6/MAC addresses and zero DHCP-option-12 hostname ("plc-secret-host" absent). Vendor name "Ubiquiti" preserved — explicitly allowed Low-BCSI per docs/audits/scrub-audit-cip011.md. Not run end-to-end through live --ai+mock-claude (would require shimming PATH), but the scrub layer that feeds the AI provider was exercised end-to-end with leak-detector regex equivalents. |
| HS-004 | yes (must-pass) | 0.90 | grep across .factory/ and docs/ yields BC-AUDIT-001..015 plus a single narrative mention of BC-AUDIT-016 in `.factory/stories/S-1.01...md` which explicitly explains "we deliberately keep this within BC-AUDIT-008 instead of adding a separate BC-AUDIT-016". BC-INDEX.md has full alias table for 001..015. The one BC-AUDIT-016 reference is a meta-narrative (about an ID intentionally NOT created), not an orphan claim. A strict regex check would flag this; a semantic read would not. |
| HS-009 | should-pass | 0.85 | -v stderr against 4SICS-GeekLounge-151020.pcap (239,267 packets) produces 5 lines. All lines match allowed shapes: version banner, two `[parse] processed N packets / X (B|MB)` heartbeats (matching the BC-9.04.001 shape), the pre-existing `parsed N packets, M hosts, K flows` summary, and the final `wrote ...` summary. Zero IPv4-, IPv6-, or MAC-shaped substrings. `[ai]` heartbeat lines from S-5.02 were NOT exercised — would require live claude or a PATH shim; per BC-6.04.001 the documented shape is conventional. Parse-progress half of HS-009 fully verified. |

## Detailed Evidence

### HS-001 — Unexpected protocols byte fidelity
- **Fixture:** /tmp/unexp.pcap — synthetic, 35 packets, one TCP/UDP flow per port in {7070, 5223, 6881, 5228, 6667, 1194, 1935, 5060, 25, 3478, 5938}.
- **Command:** `otsniff analyze /tmp/unexp.pcap -o /tmp/unexp.html --json /tmp/unexp.json --ot-subnet 192.168.0.0/16`
- **Evidence rows:** 11 (one per port).
- **HTML caption:** "Saw flow types that shouldn't be on OT: anydesk (1 flow(s)), apns (1 flow(s)), bittorrent (1 flow(s)), gcm (1 flow(s)), irc (1 flow(s)), openvpn (1 flow(s)), rtmp (1 flow(s)), sip (1 flow(s)), smtp (1 flow(s)), stun (1 flow(s)), teamviewer (1 flow(s))." — exact match against the RULES.md `ot.unexpected_protocols` trigger-string set, no missing label, no extra label.
- **Zero-flow PCAP:** /tmp/empty-ot.pcap — produces 0 findings (no false positive).

### HS-002 — Detector rule catalog sentinel
- **Catalog:** `rules --format json` lists 20 rules; all 8 wave-1 IDs present.
- **Fixture:** /tmp/wave1.pcap — 43 packets, one trigger per wave-1 rule.
- **Per-rule fire counts on /tmp/wave1.pcap:**
  - boundary.ntp_external: 1
  - compat.ntlmv1: 1
  - compat.weak_tls_cipher: 1
  - creds.ldap_simple_bind: 1
  - creds.rdp_no_nla: 1
  - ics.dnp3_engineering: 1
  - ics.modbus_unit_id_sweep: 1
  - recon.port_scan: 1
- **Playbook field:** present and non-empty on every fired finding.
- **Caveat:** Catalog entries from `rules --format json` do not carry the playbook field; only fired findings do. HS-002 references BC-3.06.003 ("every fired finding has non-empty playbook") which is what we verified.

### HS-003 — Privacy invariant survives new extractors
- **Fixture:** /tmp/wave1-priv.pcap — DHCP option-12 hostname `plc-secret-host`, plus LDAP bind, plus external-NTP.
- **Command:** `otsniff scrub /tmp/wave1-priv.pcap -o /tmp/scrubbed.md --map /tmp/scrubmap.json --ot-subnet 10.50.0.0/16`
- **Leak checks against /tmp/scrubbed.md:**
  - IPv4 (all octets <256): 0 hits
  - IPv6: 0 real hits (3 false positives were timestamps `20:28:27` etc., not IPv6 addresses)
  - MAC: 0 hits
  - `plc-secret-host` substring: **absent**
  - `Ubiquiti` vendor: present — documented Low-BCSI exception in docs/audits/scrub-audit-cip011.md
- **Caveat:** End-to-end --ai path with mock claude not exercised; scrub-layer surface that feeds the provider tested via the equivalent `scrub` subcommand.

### HS-004 — BC-AUDIT traceability
- `grep -REoh 'BC-AUDIT-[0-9]+' .factory/ docs/` returns BC-AUDIT-001 through BC-AUDIT-016.
- BC-INDEX alias table covers BC-AUDIT-001 through BC-AUDIT-015. Each BC-AUDIT-### maps to a promoted BC-X.YY.### in BC-INDEX.
- BC-AUDIT-016 referenced only in `.factory/stories/S-1.01-reconcile-bc-audit-labels.md` line 63 in a narrative that explicitly states: "We deliberately keep this within BC-AUDIT-008 instead of adding a separate BC-AUDIT-016, to avoid renumbering downstream references." The reference is a meta-mention, not an orphan claim.
- Score 0.90 (not 1.0) because a strict regex/symmetric-difference check would flag BC-AUDIT-016. A future cleanup pass could quote or restructure the narrative to remove the false positive.

### HS-009 — Progress in -v mode without TTY produces no leak
- **Fixture:** /Users/lukeadamson/1898/otsniff/tests/fixtures/4SICS-GeekLounge-151020.pcap (239,267 packets, 7.6 MB).
- **Command:** `otsniff analyze <pcap> -v -o /tmp/v20.html --ot-subnet 192.168.0.0/16 2> /tmp/v20.stderr.log` (stderr redirected — no TTY).
- **stderr (5 lines):**
  ```
  otsniff 0.4.0-dev.1 — analyzing /Users/.../4SICS-GeekLounge-151020.pcap
  [parse] processed 100,000 packets / 2.0 MB
    parsed 239267 packets, 12 hosts, 7377 flows
  [parse] processed 239,267 packets / 7.6 MB
  wrote /tmp/v20.html (7 findings across 12 hosts)
  ```
- **Pattern match:** All lines match either the documented `[parse]` shape (BC-9.04.001) or pre-existing v0.3 summary shapes.
- **Leak scan:** zero IPv4, zero IPv6, zero MAC substrings.
- **Caveat:** `[ai]` heartbeat lines (BC-6.04.001) not exercised; would require live claude CLI or PATH shim.

## Findings & Recommendations

1. **HS-004 micro-cleanup:** Edit `.factory/stories/S-1.01-reconcile-bc-audit-labels.md` line ~63 to render `BC-AUDIT-016` in backticks-only-as-token (e.g., "~~BC-AUDIT-016~~" or "the next available ID (016)") so a strict regex audit passes cleanly. Low priority; semantically already correct.
2. **HS-003 future hardening:** Add a snapshot test that runs `analyze --ai` against a mock `claude` shim (e.g., `tests/fixtures/mock-claude.sh`) and asserts captured stdin is leak-clean, to close the live-AI path. Currently the leak detector is the only enforcement on that path; an end-to-end fixture would harden it.
3. **HS-009 future hardening:** Add an integration test that exercises the `[ai] [Ns] still working...` heartbeat against the mock claude (sleep + echo) to lock the BC-6.04.001 shape.

