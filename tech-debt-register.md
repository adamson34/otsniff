# otsniff — Technical Debt Register

All entries are sourced from adversarial reviews, wave-gate findings, or
deliberate deferrals during story delivery. Each entry includes a suggested
fix location so implementers can act without re-reading review artifacts.

Priority: HIGH / MEDIUM / LOW
Status: OPEN / IN-PROGRESS / CLOSED

---

## OPEN entries

Sourced from ADV-P1 (2026-05-23, develop tip `7c98a3a`), ADV-P2 (2026-05-24, develop tip `f8e34d7`), ADV-P3 (2026-05-26, develop tip `2ac8f2e`), ADV-P4 (2026-05-26, develop tip `1f7d4cf`), and ADV-P5 (2026-05-26, develop tip `50cab61`). See `.factory/cycles/v0.4.0-feature/adversarial-reviews/pass-{1..5}.md` for full evidence + remediation per finding.

### ADV-P5 findings (2026-05-26, develop tip 50cab61, post-F-ADV-P4 fix burst)

| ID | Priority | Source | Status | Description | Suggested fix |
|---|---|---|---|---|---|
| **F-ADV-P5-001** | **HIGH** | ADV-P5 | OPEN | PCAP basename in markdown header leaks to AI provider — `acme-plant-alpha-line3.pcap` ships plant/line/facility identifier to Claude; leak detector can't catch since basename isn't in scrub map | Pass constant `"<scrubbed>"` as `input_label` for AI-bound markdown in `run_analyze`; mirror `run_scrub` shape at `cli.rs:525`; reserve real basename for HTML-only display |
| F-ADV-P5-002 | MEDIUM | ADV-P5 (≈F-ADV-P3-010, F-ADV-P1-015) | OPEN | Audit log `input_pcap.path` carries full filesystem path — operator username + plant directory hierarchy in chain-of-custody artifact | Store only `args.input.file_name()` (basename) or hash the path; SHA-256 already identifies the file content |
| F-ADV-P5-003 | MEDIUM | ADV-P5 (≈F-ADV-P1-013) | OPEN | DNP3 parser docstring promises length validation it doesn't perform — any 13-byte buffer with sync `0x05 0x64` becomes a Pdu, driving false-positive `dnp3_engineering` Criticals | Implement link-layer length validation (`payload[2]`) or correct the docstring |
| F-ADV-P5-004 | MEDIUM | ADV-P5 (refines F-ADV-P3-006) | OPEN | `unmapped_label` salt derives from `nanos_since_epoch ⊕ pid` — both observable; brute-force search-space narrows to ~16 effective bits | Switch salt source to `getrandom`/`OsRng`; document as ephemeral kernel-CSPRNG per process |
| F-ADV-P5-005 | MEDIUM | ADV-P5 | OPEN | pulldown-cmark code-block language identifier is interpolated into `<pre><code class="language-{lang}">`; potential HTML-attribute injection vector | Add positive tests for `<pre><code class="language-X" Y>` injection; confirm pulldown-cmark escapes class attr (and pin version) or sanitise the language token |
| F-ADV-P5-006 | MEDIUM | ADV-P5 (refines F-ADV-P3-004) | OPEN | `scrub_text` regex-fallback path uses the known-buggy sequential `String::replace` algorithm — unreachable today but semantically weaker; future pseudonym families with unescaped metachars would activate it | Remove fallback (panic with "regex construction must not fail") OR rewrite as single-pass longest-match manual scan |
| F-ADV-P5-007 | LOW | ADV-P5 | OPEN | `ScrubMap::validate` checks duplicate real values but not duplicate pseudonym keys across `ips`/`macs`/`names`; corrupted maps silently produce wrong unscrubbed text even under `--strict` | Add cross-family pseudonym-key uniqueness check in `validate()` |
| F-ADV-P5-008 | MEDIUM | ADV-P5 | OPEN | DHCP hostname filter allows printable ASCII including `<>'"&`; `host_label` embeds verbatim into evidence rendered by askama + sent to AI as markdown → HTML/prompt-injection vector if hostname is attacker-controlled | Reject DHCP hostnames outside `[A-Za-z0-9._-]` (RFC 952/1123 grammar) at parser, or strip at `host_label` |
| F-ADV-P5-009 | MEDIUM | ADV-P5 (refines F-ADV-P3-007 + F-ADV-P4-002) | OPEN | Mutation kill-rate threshold 79.1% is 5pp below 84.1% baseline; no ratchet, no per-run delta — drift to 80% passes gate silently while codebase regresses 4pp | Tighten threshold to baseline minus 1pp OR persist previous-run rate as artifact and fail on >2pp drop; atomic baseline updates |
| F-ADV-P5-010 | MEDIUM | ADV-P5 | OPEN | Fuzz floor "10 runs OR 1 pulse" is so low that a regression making `scrub_text` quadratic still passes; libFuzzer always emits `#0 INITED` so effective pulse floor is ≈0 | Compute per-harness minimum-runs threshold from baseline (parser fuzz ≥100k runs/60s); persist; fail on >50% drop |
| F-ADV-P5-011 | LOW | ADV-P5 | OPEN | `audit::sha256_file_hex` maps mid-file read errors to `OtError::InputOpen` (exit 2) — exit code semantics imprecise | Add `OtError::InputRead { path, source }` variant or reuse `OtError::Parse` for read-mid-stream |
| F-ADV-P5-012 | LOW | ADV-P5 (refines F-ADV-P3-011) | OPEN | S7 `cotp_len_byte` cap of 17 is over twice realistic COTP ceiling (RFC 905 class-0 max ≈6, class-2 with TPDU-size ≈8); attacker still has 12 free bytes to position synthetic `0x32` | Tighten cap to 8 |
| F-ADV-P5-013 | LOW | ADV-P5 | OPEN | `ldap_starttls_flows` HashMap inserts but never expunges; long captures with many ephemeral src ports inflate map unboundedly — adversarial port-scan capture pushes into multi-GB | Cap map (LRU or hard ceiling 10,000 entries); expunge entries with observed bind |
| F-ADV-P5-014 | MEDIUM | ADV-P5 (refines F-ADV-P4-005) | OPEN | `kani.yml` count uses `steps.*.outcome` (generic GHA exit code) — `cargo kani --harness foo_not_existing` exits 0 with "no harness matched" in some versions; expected count 8 is hardcoded so drift is silent | Parse each step's stdout for `VERIFICATION SUCCESSFUL`; assert per-harness count of verified properties non-zero; derive expected count from `grep -c #[kani::proof]` |
| F-ADV-P5-015 | LOW | ADV-P5 | OPEN | `f64::MAX` accepted as `--flow-shift-multiplier` (finite + ≥1); diff produces `0 flow_shifts` silently — operator footgun with no signal | Add upper-bound check `> 1e6` |
| F-ADV-P5-016 | MEDIUM | ADV-P5 | OPEN | Audit log `command` field interpolates `args.model` raw; "command" string diverges from `Command::new(...).args([...])` argv invocation | Validate `args.model` to `[A-Za-z0-9._-]+`, OR rename field to `argv` and serialize as JSON array |
| F-ADV-P5-017 | LOW | ADV-P5 | OPEN | No integration test exercises the audit-log `ensure_clean` + `ensure_no_map_values` at run-time; only synthetic unit test exists | Add `tests/snapshot.rs` or `tests/cli_smoke.rs` integration test that runs `analyze --ai` with mocked AI provider and asserts leak detector pass on the audit log file |

### ADV-P3 findings (2026-05-26, develop tip 2ac8f2e, post-F-ADV-P2 fix burst)

| ID | Priority | Source | Status | Description | Suggested fix |
|---|---|---|---|---|---|
| F-ADV-P3-008 | MEDIUM | ADV-P3 | OPEN | `kani.yml` runs only on weekly cron + manual dispatch; composed privacy proof is not gated on PR → privacy-affecting regression could land for ≤7 days before detection | Add `pull_request:` trigger with `paths:` filter for scrub/leak_detector/diff/html_render/kani_proofs |
| F-ADV-P3-009 | MEDIUM | ADV-P3 (related F-ADV-P1-019) | OPEN | `which_claude` checks `is_file()` but not Unix executable bit; non-executable file at $PATH/claude passes check then `cmd.spawn()` fails cryptically | Check `meta.permissions().mode() & 0o111 != 0` on Unix; expand candidate names on Windows |
| F-ADV-P3-010 | MEDIUM | ADV-P3 (≈F-ADV-P1-015) | OPEN | `audit::AuditLog.input_pcap.path` records full filesystem path — same defect as F-ADV-P2-009 fixed for markdown header, but audit log was not updated; chain-of-custody artifact leaks operator username + plant name | Record only basename + SHA-256 (already present); drop the full path |
| F-ADV-P3-011 | MEDIUM | ADV-P3 | OPEN | `parse::s7comm` doesn't bound `cotp_len_byte`; RFC 905 COTP class-0 header max is ~6 bytes, accepting up to 255 gives malicious packet 248 free bytes to position synthetic `0x32` at S7 offset | Add `if cotp_len_byte > 17 { return None; }` early bound |
| F-ADV-P3-012 | MEDIUM | ADV-P3 (≈F-ADV-P1-008) | OPEN | `diff::compute` EC-002 warning silenced when either map empty (`!base_pseudo_set.is_empty() && !curr_pseudo_set.is_empty()`); empty map is a more plausible bug than non-empty disjoint | Add separate empty-map warning before the disjointness check |

### ADV-P2 still-OPEN (post-PR #100)

| ID | Priority | Source | Status | Description | Suggested fix |
|---|---|---|---|---|---|
| F-ADV-P2-010 | MEDIUM | ADV-P2 | OPEN | DHCP hostname filter drops non-ASCII bytes silently — `LINE-3-Ümlaut` → `LINE-3-mlaut`; breaks merge-map identity; 1-letter survivors become regex-friendly scrub targets | Decode via `String::from_utf8_lossy`; reject if any control character or shorter than 2 graphemes |
| F-ADV-P2-012 | MEDIUM | ADV-P2 | OPEN | `ipv6_regex` blind to `::1`, `fe80::1`, `2001:db8::`, `::ffff:192.0.2.1`, zoned `fe80::1%eth0` — leak detector blind spot | Use `Ipv6Addr::from_str` on colon-containing tokens; add tests for the missed forms |
| F-ADV-P2-013 | MEDIUM | ADV-P2 | OPEN | `ensure_no_map_values` blocks on first hit; multi-leak inputs under-reported; substring matching false-positives on short (< 4 char) real values | Collect all leaks into Vec; iterate reverse-length order; skip real values < 4 chars |
| F-ADV-P2-016 | LOW | ADV-P2 | OPEN | `unscrub_text` regex unbounded digit suffix; `unmapped` uses O(n²) Vec::contains | Limit suffix to `[0-9]{1,9}`; use `HashSet<String>` for unmapped |
| F-ADV-P2-017 | LOW | ADV-P2 | OPEN | cargo-deny CI step has no positive-coverage assertion (extension of POL-11 theme) | Configure with explicit command + arguments; grep advisory-count line |
| F-ADV-P2-018 | LOW | ADV-P2 | OPEN | `--review-scrub` prints full scrubbed payload to stderr → terminal history / session logs (tmux/screen) capture it | Write payload to temp file; print path + first/last 20 lines |
| F-ADV-P2-019 | LOW | ADV-P2 | OPEN | `which_claude` skips Windows `.exe`/`.cmd`/`.bat` — false-negative pre-flight on shipped Windows target | Use `which` crate or expand candidate names on Windows |
| F-ADV-P2-020 | LOW | ADV-P2 | OPEN | ENIP `engineering_class_cip` heuristic sweeps fixed offset range; can flag benign CPF item bytes (0x05, 0x06, 0x07) as engineering services | Decode CPF item structure properly; restrict scan window |

### ADV-P1 still-OPEN (from 2026-05-23 pass)

| ID | Priority | Source | Status | Description | Suggested fix |
|---|---|---|---|---|---|
| F-ADV-P1-006 | MEDIUM | ADV-P1 | OPEN | `unscrub` writes the AI's response with no leak check — asymmetric to `analyze --ai` flow that guards going TO the AI | Run `leak_detector::ensure_clean(&output)` after `unscrub_text`; refuse to write or at minimum warn |
| F-ADV-P1-007 | MEDIUM | ADV-P1 | OPEN | Diff output is never `ensure_clean`-checked after rendering — relies entirely on `scrub_finding` being comprehensive (F-ADV-P1-003 shows it's not) | After rendering `content` in `run_diff`, run `ensure_clean` + `ensure_no_map_values` against both maps; fail closed |
| F-ADV-P1-008 | MEDIUM | ADV-P1 | OPEN | `diff::compute` disjoint-maps WARNING fires on legitimate first-runs and pollutes CI logs | Add `--disjoint-ok` opt-in OR tag the warning with a noise-suppression key |
| F-ADV-P1-009 | MEDIUM | ADV-P1 | OPEN | `scrub_text` longest-first sort handles containment for IPs but not pseudonyms-containing-pseudonyms; no post-sub assertion of leak-free output | Run `ensure_no_map_values(&out, map)` inside `scrub_text` and panic / return Err if a real value survives |
| F-ADV-P1-010 | MEDIUM | ADV-P1 | OPEN | `kani.yml` has no positive-coverage assertion — `continue-on-error: true` per harness means a silently-dropped harness can still report green (POL-11) | Compute harness count at runtime via `tojson`/`fromJSON`; emit `Check passed: N of M harnesses succeeded` |
| F-ADV-P1-011 | MEDIUM | ADV-P1 | OPEN | `fuzz.yml` has no positive-coverage assertion — `cargo fuzz run` success is exit-0 only (POL-11) | Grep stdout for `#NNN INITED` lines; assert at least one is present; emit `Check passed: harness $h ran $count executions` |
| F-ADV-P1-012 | LOW | ADV-P1 | OPEN | `recon.port_scan`'s `is_broadcast_or_multicast` only catches `255.255.255.255` — subnet-directed broadcast (`.255` on /24) is not excluded → false positives on NetBIOS browse traffic | Either skip last-octet `.255` heuristic, or reword rule trigger to clarify only limited-broadcast/multicast are excluded |
| F-ADV-P1-013 | MEDIUM | ADV-P1 | OPEN | `dnp3::parse` ignores frame `length` field and reads `payload[12]` as function code unconditionally — random bytes can mis-classify as engineering ops on short frames | Read length at offset 2, verify ≥5, compute application offset more carefully, reject if FIR bit not set on first segment |
| F-ADV-P1-014 | MEDIUM | ADV-P1 | OPEN | `render_safe` does NOT strip `javascript:` / `data:` URLs from markdown links — test asserts the opposite of its name | Add post-pass that walks generated HTML and replaces `href` values starting with `javascript:`/`data:`/`vbscript:` with `#`; flip the test |
| F-ADV-P1-015 | LOW | ADV-P1 | OPEN | `audit.path` JSON field contains the input PCAP's full path including user-home directory — chain-of-custody artifact leaks operator identity (POL-12 runtime variant) | Normalise to basename + SHA-256 (already present) OR add `--audit-anonymise-path` flag |
| F-ADV-P1-016 | LOW | ADV-P1 | OPEN | `pcap::iter_packets` silently drops non-Ethernet2 link types (VLAN-tagged trunks, 802.11) after first packet — incomplete inventory without warning | Either handle `LinkSlice::EthernetWithVlan`, or stderr-warn on first dropped frame |
| F-ADV-P1-017 | MEDIUM | ADV-P1 | OPEN | `scrub::merge_map` can `panic!` on a corrupted on-disk baseline map (EC-002 path) — should be `Err(OtError::Parse(...))` per existing error-handling convention | Replace `panic!` with `Err(OtError::Parse(...))`; treat scrub-map corruption as bad input (exit code 2) not a crash |
| F-ADV-P1-018 | LOW | ADV-P1 | OPEN | `scrub_finding` doesn't scrub `Finding.id` or `Finding.recommendation` — safe today (`&'static str`) but fragile to future field changes | Pair with F-ADV-P1-007 (post-render `ensure_clean`) for defense in depth, or add static_assertion on the field type |

---

## CLOSED entries

| ID | Priority | Source | Closed | Resolution |
|---|---|---|---|---|
| F-W1-001 | HIGH | Wave-1 adversarial review | 2026-05-21 (PR #86, 7d8413e) | Added `map.validate()?` after `serde_json::from_slice` in `run_unscrub`. CLI smoke test `test_f_w1_001_unscrub_rejects_corrupted_map` exercises the rejection path. |
| F-W1-002 | HIGH | Wave-1 adversarial review | 2026-05-22 (PR #87, 80a2e91) | Tightened `pseudonym_regex()` from `[0-9a-f]+` to `[0-9]+` to match `build_map`'s `{:03}` decimal output. New tests `test_f_w1_002_pseudonym_regex_rejects_hex_only_suffix` and `test_f_w1_002_decimal_pseudonym_not_in_map_is_still_unknown` cover the correctness gap and the strict-mode regression guard. |
| F-W1-003 | MEDIUM | Wave-1 adversarial review | 2026-05-22 (bundled PR fix/F-W1-003-004-scrub-cleanup) | Extended `ScrubMap::validate()` with a second pass that detects duplicate real-value entries across `ips`/`macs`/`names`. Three new tests cover same-family duplicates, cross-family duplicates, and the regression guard for unique-value maps. |
| F-W1-004 | MEDIUM | Wave-1 adversarial review | 2026-05-22 (bundled PR fix/F-W1-003-004-scrub-cleanup) | Wrapped `IPV4_RE`/`IPV6_RE`/`MAC_RE` in `std::sync::LazyLock` (stable since 1.80; MSRV 1.85). Regexes now compile exactly once per process instead of on every `scan()` call. Public regex-helper signatures changed to return `&'static Regex`. |
| F-W1-005 | MEDIUM | Wave-1 Kani stories (S-4.01..03) | 2026-05-20 (PR #84, f068948) | Kani CI workflow_dispatch executed; first run surfaced all 5 harnesses failed/timed out. Proof-model rewrite landed; all 6 harnesses now report `VERIFICATION:- SUCCESSFUL` on develop CI. |
| F-ADV-P1-001 | HIGH | ADV-P1 | 2026-05-23 (PR #99, f8e34d7) | Added `--ot-subnet` (repeatable) to the `Diff` clap variant; threaded into `run_diff`; calls `ot_or_default(&user_supplied)` instead of hardcoded RFC1918. Test `test_f_adv_p1_001_diff_documents_ot_subnet_flag` verifies the flag appears in `--help`. |
| F-ADV-P1-002 | HIGH | ADV-P1 | 2026-05-23 (PR #99, f8e34d7) | Added `compute_with_multiplier(input, input, multiplier)` as new public function; `compute()` is now a thin wrapper using `DEFAULT_FLOW_SHIFT_MULTIPLIER`. CLI passes user-supplied value directly to compute, removing the broken post-filter. Parse-time validation rejects multiplier < 1.0 or non-finite. Test `test_f_adv_p1_002_flow_shift_multiplier_below_default_retains_flows` verifies a 1.7× flow appears in `flow_shifts` when user passes 1.5. |
| F-ADV-P1-003 | HIGH | ADV-P1 | 2026-05-23 (PR #99, f8e34d7) | Changed `src/findings/ldap_creds.rs:93` evidence format from Unicode `→` to ASCII `->` for diff-extractor compatibility (matches the convention used by every other detector). Test `test_f_adv_p1_003_ldap_creds_evidence_uses_ascii_arrow` builds an LDAP bind fixture and asserts `->` present + `→` absent. |
| F-ADV-P1-004 | HIGH | ADV-P1 | 2026-05-23 (PR #99, f8e34d7) | Rewrote `fuzz/fuzz_targets/scrub_text.rs` to derive a small ScrubMap from fuzzer bytes (one fixed `host_000` entry guaranteeing substitution always runs + two carved-from-data entries). `validate()` map before scrubbing. Test `test_f_adv_p1_004_scrub_fuzz_harness_uses_non_empty_map` does structural check. |
| F-ADV-P1-005 | HIGH | ADV-P1 | 2026-05-23 (PR #99, f8e34d7) | Rewrote composed Kani harness to assert two non-trivial properties: (1) vacuous-case idempotence (scrub-then-scrub on clean input is identity) and (2) leak-detector structural soundness via slice-equality-based independent check (structurally different from `byte_contains_model`'s manual loop). Added preconditions matching production `build_map` invariants (real ⊄ pseudo, pseudo ⊄ real). Updated `docs/proofs/privacy-invariant.md` with "Honest scope" section acknowledging the rewrite and the deferred model-to-production gap (now covered by F-ADV-P1-004's working fuzz harness). |
| F-ADV-P2-001 | **CRITICAL** | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | Added URL-scheme sanitiser to `render_safe`: walks pulldown-cmark events and rewrites link + image `dest_url` to `#` when scheme is `javascript:`/`data:`/`vbscript:` (case-insensitive, whitespace-trim resistant). Flipped the upside-down test from "asserts links pass through" to "asserts links are stripped"; added 5 new regression tests covering data:/vbscript:/mixed-case/whitespace-evasion/image-src vectors. |
| F-ADV-P2-002 | **CRITICAL** | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | Added `leak_detector::ensure_clean(&content)` + `ensure_no_map_values` against both maps before `std::fs::write` in `run_diff`. Replaced `ip_to_pseudo`'s `.unwrap_or(ip_str)` raw-IP fallback AND `HostRef::from_asset`'s `format!("unmapped:{}", ip)` fallback with new `unmapped_label()` helper that returns `unmapped_<sha256-prefix>` opaque labels. Defense-in-depth via the post-render leak gates. |
| F-ADV-P2-003 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | New `composed_privacy_invariant_non_vacuous` Kani harness proves the load-bearing branch (scrub replaces real → output does not contain real). Takes symbolic real, asserts build_map preconditions, runs `replace_first_model(real, real, pseudo)`, asserts `byte_contains_model(out, real) == false`. Local `cargo kani`: VERIFICATION:- SUCCESSFUL in 2.9s. Wired into kani.yml. |
| F-ADV-P2-004 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | Added `OtError::PrivacyLeak { kind, message }` variant with distinct exit code 75 (EX_TEMPFAIL). Migrated `leak_detector::ensure_clean` + `ensure_no_map_values` to the new variant. CI scripts can now branch on privacy-invariant trips vs render bugs. Existing leak-detector tests updated. |
| F-ADV-P2-005 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | `scrub_text` fuzz harness now calls `ensure_no_map_values(&scrubbed, &map)` after `scrub_text` and panics on Err (libfuzzer records it). Skip-condition for degenerate maps where real is a substring of its own pseudonym. |
| F-ADV-P2-006 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | `fuzz.yml` captures cargo-fuzz stdout, greps for libFuzzer pulse lines + `Done N runs` summary, requires ≥1 pulse OR ≥10 runs, echoes `Check passed: $PULSES pulse line(s), $RUNS runs in 60s` to `$GITHUB_STEP_SUMMARY`. POL-11 compliant. |
| F-ADV-P2-007 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | Leak-detector error messages now redact the raw value: replaced `'{}'` with length + byte_offset + 4-char SHA-256 hash prefix via new `leak_hash_prefix()` helper. Stderr / CI-log egress path closed. |
| F-ADV-P2-008 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | `build_map` now sweeps `obs.mac_frame_counts.keys()` in addition to host-associated MACs. Passive-observer MAC / SVI / VRRP virtual MACs that aren't attached to any host's IP get pseudonyms appended after the host-MAC numbering, preserving round-trip stability. |
| F-ADV-P2-009 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | `render_markdown` now receives `args.input.file_name()` (basename only) instead of `args.input.display().to_string()` (full path). Username + plant name + embedded path-IPs no longer leak to the AI provider via the markdown header. Audit log retains SHA-256 for chain-of-custody. |
| F-ADV-P2-014 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | Pinned `perf.yml`'s `actions/upload-artifact` from non-existent `@v7` to `@v4` (matches fuzz.yml and mutants.yml). |
| F-ADV-P2-015 | HIGH | ADV-P2 | 2026-05-24 (PR #100, 2ac8f2e) | Switched all 7 fixture-dependent CLI smoke tests from `tests/fixtures/Modbus.pcap` (gitignored) to `tests/fixtures/synthetic-1mb.pcap` (committed via .gitignore exception). Added `assert!(env::var("CI").is_err())` inside each skip branch so missing fixture in CI is a hard failure, not silent skip. Updated `scrub_round_trip_via_pcap` assertions for synthetic IPs (10.10.0.*). |
| F-ADV-P3-001 | **CRITICAL** | ADV-P3 | 2026-05-26 (PR #101, 1f7d4cf) | `run_scrub` now calls `leak_detector::ensure_clean(&md)` + `ensure_no_map_values(&md, &map)` immediately after `scrub_text` and before either write. The scrub subcommand is now symmetric with `analyze --ai` and `diff` in its fail-closed privacy posture. Regression test `test_f_adv_p3_001_run_scrub_output_passes_leak_detector`. |
| F-ADV-P3-002 | HIGH | ADV-P3 | 2026-05-26 (PR #101, 1f7d4cf) | Added `debug_assert!` in `build_map` (post-map construction) that every flow-key IP is in `ips`. Catches in debug builds the case where the observer populates `obs.flows` with an IP not in `obs.hosts`. The fail-closed leak gate (F-ADV-P3-001 / F-ADV-P2-002) catches it in release. |
| F-ADV-P3-003 | HIGH | ADV-P3 (partial F-ADV-P2-001 completion) | 2026-05-26 (PR #101, 1f7d4cf) | `url_is_unsafe` now strips ALL ASCII whitespace + control characters (not just leading) before scheme comparison, matching WHATWG URL §4.4.3 normalization. 4 new tests + direct unit test of `url_is_unsafe` against embedded-tab/LF/CR + angle-bracket syntax. |
| F-ADV-P3-004 | HIGH | ADV-P3 | 2026-05-26 (PR #101, 1f7d4cf) | `scrub_text` rewritten as single-pass regex alternation (`Regex::replace_all` with longest-first ordering). Cursor advances past the entire matched span — no shadowing when a real value is a prefix of any pseudonym. Regression test `test_f_adv_p3_004_scrub_text_no_shadowing_when_real_is_pseudonym_prefix` exercises the `host`/`host_001` corruption scenario. Fallback to sequential implementation if regex construction fails (theoretical only). |
| F-ADV-P3-005 | HIGH | ADV-P3 | 2026-05-26 (PR #101, 1f7d4cf) | `ScrubMap::validate` now iterates by family with the canonical prefix (`host_`, `mac_`, `name_`) and calls `is_canonical_pseudonym(pseudo, prefix)` (new helper) requiring suffix matches `\d+`. Rejects `"FOOBAR": "10.0.0.1"` etc. with descriptive error. Tests: `test_f_adv_p3_005_validate_rejects_non_canonical_pseudonym` + positive regression guard. |
| F-ADV-P3-006 | HIGH | ADV-P3 (partial F-ADV-P2-002 completion) | 2026-05-26 (PR #101, 1f7d4cf) | `unmapped_label` now uses 16 hex chars (64 bits of SHA-256 prefix, up from 16 bits) plus a per-process random salt (`UNMAPPED_SALT` LazyLock seeded from system time + PID). `OTSNIFF_UNMAPPED_SALT` env var escape hatch for deterministic tests. Brute-force cost: O(2^16) → O(2^64); rainbow tables no longer reusable across runs. Tests assert ≥16-char hex suffix. |
| F-ADV-P3-007 | HIGH | ADV-P3 | 2026-05-26 (PR #101, 1f7d4cf) | `mutants.yml` now `exit 1` if `KILL_PCT < 79.1` (5pp below 84.1% baseline). Positive-coverage `echo "Check passed: kill rate X% (>= 79.1%)..."` on success. POL-11 compliant. |
| F-ADV-P4-001 | **CRITICAL** | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | Split LDAP observation into two direction-keyed branches. `is_ldap_src` (response, server→client) detects STARTTLS success and stores under (client, server, client_port, server_port) tuple. `is_ldap_dst` (request, client→server) reads from the same tuple. Suppression now actually works in production. Regression test constructs both packets at Packet level. |
| F-ADV-P4-002 | HIGH | ADV-P4 (partial F-ADV-P3-007 completion) | 2026-05-26 (PR #102, 50cab61) | Updated mutants.yml parser to cargo-mutants v27 schema (CaughtMutant / MissedMutation / Timeout / Unviable). Added schema-mismatch sanity check: if outcomes.json has entries but parser found zero of any kind, fail with `::error::F-ADV-P4-002`. Killed counts include Timeout; Unviable excluded from denominator. Positive-coverage line `Check passed: parsed N outcomes...`. |
| F-ADV-P4-003 | MEDIUM | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `extract_kv` test-helper branch now requires ALL THREE tokens (src=, dst=, port=) present and non-empty before taking it. Future production evidence with stray `port=NNN` token will route through the real regex path. |
| F-ADV-P4-004 | MEDIUM | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `composed_privacy_invariant_non_vacuous` harness now asserts inline `out_len == pseudo.len()` and per-byte `out_slice[k] == pseudo[k]`. Removes the cross-harness antecedent. Re-verified: VERIFICATION:- SUCCESSFUL. |
| F-ADV-P4-005 | MEDIUM | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `kani.yml` counts successful harnesses and asserts equality with EXPECTED=8. Emits `Check passed: 8/8 Kani harnesses verified`. POL-11 compliant. |
| F-ADV-P4-006 | MEDIUM | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | S7 parser now computes `s7_header_len` first (after the 2-byte minimum to read protocol-ID + rosctr), then size-dependent guard `payload.len() >= s7_offset + s7_header_len + 1`. Plus F-ADV-P3-011 bound on cotp_len_byte (max 17 per RFC 905 class-0). |
| F-ADV-P4-007 | MEDIUM | ADV-P4 (escalated from LOW) | 2026-05-26 (PR #102, 50cab61) | ENIP CIP scan now parses CPF (Common Packet Format) item structure properly. Reads item count, walks items with (type_id, length) pairs, inspects only Connected (0x00B1) or Unconnected (0x00B2) data items, reads service code at documented offset (0 for Unconnected, 2 after sequence count for Connected). 24-byte window false-positive vector eliminated. |
| F-ADV-P4-008 | MEDIUM | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `run_unscrub` now emits stderr `WARNING (F-ADV-P4-008): scrub map has zero entries...` when `map.is_empty()`. Promoted to `OtError::Parse` under `--strict`. Regression test confirms the strict-mode rejection. |
| F-ADV-P4-009 | LOW | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `merge_family` returns `Result<()>`; `merge_map` returns `Result<ScrubMap>`. EC-002 path returns `OtError::Parse(...)` with a descriptive message. CLI caller uses `?` propagation. 15 in-file test callers updated to `.expect("merge_map should succeed on test fixture")`. |
| F-ADV-P4-010 | MEDIUM | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `run_diff` computes % of observed hosts resolvable in each side's map; emits `WARNING (F-ADV-P4-010): only N% of {side} hosts covered by --{side}-map...` when below 50%. Catches the swapped --baseline-map / --current-map operator footgun. |
| F-ADV-P4-011 | LOW | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `leak_regex_mac` Kani harness now takes a symbolic `uppercase: bool = kani::any()` that applies uniformly to all nibbles. Proves the model recognises both all-lowercase AND all-uppercase MACs. Re-verified SUCCESSFUL. Mixed-case is documented as fuzz-verified gap. |
| F-ADV-P4-012 | LOW | ADV-P4 | 2026-05-26 (PR #102, 50cab61) | `pseudonym_regex` retains `\b` boundary (Rust regex crate has no lookahead). Realistic separators (space/comma/newline) work correctly. Zero-separator case `host_001host_002` is documented as a limitation in tests; only the first pseudonym resolves. |
